{
  "name": "chatgpt-conversation-search",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local Chromium extension for fast ChatGPT conversation title search.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check src/contentScript.js && node --check src/serviceWorker.js && node --check src/popup.js"
  }
}
